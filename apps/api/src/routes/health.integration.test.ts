import { describe, expect, it } from 'vitest';

describe('health endpoints', () => {
  it('livez returns alive', async () => {
    // TODO: start Fastify app and assert /livez
    expect(true).toBe(true);
  });
});
